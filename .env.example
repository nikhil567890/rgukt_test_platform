# ==========================================
# SERVER-ONLY ENVIRONMENT VARIABLES
# (Configure in Vercel / Cloud Run Project Settings - NEVER expose to browser)
# ==========================================

# Persistent Database connection string (PostgreSQL required for production serverless)
# Example: postgresql://username:password@db-host.supabase.co:5432/postgres?sslmode=require
DATABASE_URL=postgresql://neondb_owner:npg_SqH2kRC7spWU@ep-billowing-base-aew707y4-pooler.c-2.us-east-2.aws.neon.tech/rguktdb?sslmode=require&channel_binding=require

# Cryptographically strong JWT Secret Key for token signing (min 32 characters)
# Required in production. Generate using: openssl rand -base64 32
JWT_SECRET=48a53959e3a5ff34e679a7792be147b610e714b3fe06195f811dfd4712c930e9

# (Optional) Razorpay payment gateway credentials
RAZORPAY_KEY_ID=
RAZORPAY_KEY_SECRET=
PREMIUM_AMOUNT_PAISE=300000

# (Optional) Google Gemini AI API key for question analysis and AI explanations
GEMINI_API_KEY=

# ==========================================
# PUBLIC CLIENT ENVIRONMENT VARIABLES
# (Safe for browser - prefixed with VITE_)
# ==========================================

# (Optional) Custom API URL if frontend and backend are hosted on different domains.
# Leave blank when deploying unified application on Vercel or Cloud Run.
VITE_API_URL=

# Firebase Client Web SDK configuration
VITE_FIREBASE_API_KEY=
VITE_FIREBASE_AUTH_DOMAIN=
VITE_FIREBASE_PROJECT_ID=
VITE_FIREBASE_STORAGE_BUCKET=
VITE_FIREBASE_MESSAGING_SENDER_ID=
VITE_FIREBASE_APP_ID=
VITE_FIREBASE_MEASUREMENT_ID=

